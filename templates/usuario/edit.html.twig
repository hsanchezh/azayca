{% extends 'layout.html.twig' %}

{% block title %}{{ navega_editar_usuario }}{% endblock %}

{% block body %}
    <h1>{{ navega_editar_usuario }}</h1>

    {{ include('usuario/_form.html.twig', {'button_label': actualizar}) }}

    <a href="{{ path('app_usuario_index') }}" class="{{ estilo_boton_volver }}">{{ volver_al_listado }}</a>

    {{ include('usuario/_delete_form.html.twig') }}
{% endblock %}
