{% extends 'layout.html.twig' %}

{% block title %}{{ navega_editar_viaje }}{% endblock %}

{% block cabecera %}
    <h3 class="{{ estilo_cabecera_pagina }}">{{ navega_editar_viaje }}</h3>
{% endblock %}

{% block contenido %}
    {{ include('viaje/_form.html.twig', {'button_label': actualizar}) }}
{% endblock %}

{% block acciones %}
    <a href="{{ path('app_viaje_index') }}" class="{{ estilo_boton_volver }}">{{ volver_al_listado }}</a>

    {{ include('viaje/_delete_form.html.twig') }}
{% endblock %}
