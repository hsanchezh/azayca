<form method="post" action="{{ path('app_viaje_delete', {'id': viaje.id}) }}" onsubmit="return confirm('{{ confirmar_borrado_viaje }}');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ viaje.id) }}">
    <button class="{{ estilo_boton_eliminar }}">{{ eliminar }}</button>
</form>
