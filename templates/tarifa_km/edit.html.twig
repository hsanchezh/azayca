{% extends 'layout.html.twig' %}

{% block title %}{{ navega_editar_tarifa_km }}{% endblock %}

{% block cabecera %}
    <h3 class="{{ estilo_cabecera_pagina }}">{{ navega_editar_tarifa_km }}</h3>
{% endblock %}

{% block contenido %}
    {{ include('tarifa_km/_form.html.twig', {'button_label': actualizar}) }}

    <h5>{{ navega_modifica_viajes_con_esta_tarifa }}</h5>
    <div class="{{ estilo_subcontenido }}">
        {{ include('viaje/_tabla_viajes.html.twig') }}
    </div>
{% endblock %}

{% block acciones %}
    <a href="{{ path('app_tarifa_km_index') }}" class="{{ estilo_boton_volver }}">{{ volver_al_listado }}</a>

{# No se puede eliminar una tarifa
        {% if not viajes %}
            {{ include('tarifa_km/_delete_form.html.twig') }}
        {% endif %}
#}
{% endblock %}
