{% extends 'layout.html.twig' %}

{% block title %}{{ navega_editar_paciente }}{% endblock %}

{% block cabecera %}
    <h3 class="{{ estilo_cabecera_pagina }}">{{ navega_editar_paciente }}</h3>
{% endblock %}

{% block contenido %}
    {{ include('paciente/_form.html.twig', {'button_label': actualizar}) }}
{% endblock %}

{% block acciones %}
    <a href="{{ path('app_paciente_index') }}" class="{{ estilo_boton_volver }}">{{ volver_al_listado }}</a>

    {{ include('paciente/_delete_form.html.twig') }}
{% endblock %}
