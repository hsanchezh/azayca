{% extends 'layout.html.twig' %}

{% block title %}{{ navega_editar_conductor }}{% endblock %}

{% block cabecera %}
    <h3 class="{{ estilo_cabecera_pagina }}">{{ navega_editar_conductor }}</h3>
{% endblock %}

{% block contenido %}
    {{ include('conductor/_form.html.twig', {'button_label': actualizar }) }}

{% endblock%}

{% block acciones %}

    <a href="{{ path('app_conductor_index') }}" class="{{ estilo_boton_volver }}">{{ volver_al_listado }}</a>

    {{ include('conductor/_delete_form.html.twig') }}
{% endblock %}
